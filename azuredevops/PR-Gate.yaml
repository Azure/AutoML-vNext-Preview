variables:
- name: SubscriptionName
  value: 'INFRA Project Vienna INT (589c7ae9-223e-45e3-a191-98433e0821a9)'

pr:
- main

trigger: none # No CI build

pool:
  vmImage: 'ubuntu-latest'

jobs:
- job: RunTests
  displayName: Run Tests
  pool:
    vmImage: ubuntu-latest
  steps:
  - checkout: self
    fetchDepth: 1
    clean: true

  - task: UsePythonVersion@0
    displayName: 'Use Python 3.6'
    inputs:
      versionSpec: '3.6'

  - script: |
      echo "##[section]Upgrading pip and setuptools..."
      pip install --upgrade pip setuptools
    failOnStderr: true
    displayName: Install pip & setuptools

  - task: AzureCLI@2
    displayName: Do something Azure
    inputs:
      azureSubscription: '$(SubscriptionName)'
      scriptType: pscore
      scriptLocation: inlineScript
      failOnStandardError: false
      inlineScript: |
        echo "##[section]Running a command"
        az account list